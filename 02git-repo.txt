git config --list
git config --global user.name "Your Name"
git config --global user.email "name@example.com"

git clone https://github.com/sharvinsoham/git-repo.git

code .\git-repo\

git add .
git add filename #adds the file into staging area

git status Tells #status about files whether committed or not
git commit -m "Contains comments about changes"
git push